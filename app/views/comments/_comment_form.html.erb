<%= form_with(model: [@opinion, @opinion.comments.new], local: true) do |f|%>
    <%= f.label :content, hidden: true %>
    <%= f.text_area :content, placeholder: 'Leave a comment' %>

    <%= f.submit 'Done' %>
<% end %>